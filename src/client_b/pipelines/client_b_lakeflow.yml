# Generated by ipac_delta_sync for client: client_b
# Client B Dev7 POC — additional tables only
# uc_catalog: ipac_tax_synch (from databricks.yml var.uc_catalog)
# cluster_tier: 4 (tier_4) — Higher priority / wider tables (workers 4-6)
# staging: ipac_tax_synch.client_b_raw (volume auto-created in raw schema)
# raw + staging schema: ipac_tax_synch.client_b_raw
# tables: 1 | continuous Lakeflow Connect CDC
# Regenerate: ipac-delta-sync generate --client client_b
resources:
  pipelines:
    client_b_lakeflow_cdc:
      name: client_b_lakeflow_cdc
      pipeline_type: MANAGED_INGESTION
      channel: PREVIEW
      serverless: true
      continuous: true
      catalog: ${var.uc_catalog}
      schema: client_b_raw
      ingestion_definition:
        connection_name: dev7_15347
        connector_type: CDC
        table_configuration:
          enable_auto_clustering: true
        data_staging_options:
          catalog_name: ${var.uc_catalog}
          schema_name: client_b_raw
        objects:
          - table:
              source_catalog: 'iPC_2025_Dev7_15347'
              source_schema: 'dbo'
              source_table: 'CYAdjustmentInput'
              destination_catalog: ${var.uc_catalog}
              destination_schema: 'client_b_raw'
              destination_table: 'CYAdjustmentInput'
              table_configuration:
                clustering_columns: [adjustment_id]
