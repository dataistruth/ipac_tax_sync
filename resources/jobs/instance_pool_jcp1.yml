# Shared warm pool for all Lakeflow Connect pipeline clusters (D8s_v3, 8 vCPU).
# Pool: min_idle_instances + max_capacity. Cluster autoscale: min/max workers (vars).
resources:
  instance_pools:
    jcp1:
      instance_pool_name: ipac_lakeflow_jcp1
      node_type_id: ${var.instance_pool_node_type_id}
      min_idle_instances: ${var.instance_pool_min_idle_instances}
      max_capacity: ${var.instance_pool_max_capacity}
      preloaded_spark_versions:
        - ${var.pipeline_spark_version}
      permissions:
        - level: CAN_MANAGE
          group_name: ${var.grant_group}
